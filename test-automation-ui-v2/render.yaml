services:
  - type: web
    name: trinamix-scheduler
    env: node
    rootDir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: BROWSER_USE_API_KEY
        sync: false   # Set this manually in the Render dashboard
      - key: NODE_VERSION
        value: "18"
